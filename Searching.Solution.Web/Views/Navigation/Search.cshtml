@*using SearchingLibrary;
@model List<Categories>*@
<div class="col-sm-2"></div>
    <div class="col-sm-2 text-center">
        <div class="btn-group-vertical" style="position:fixed; margin-top: 10px;">
            <a ui-sref=".Announcing({categories_id:{{category.Categories_id}}})" class="btn btn-default" role="button" ng-repeat="category in Categories">{{category.Name_Category}}</a>
        </div>
    </div>
    <div class="col-sm-4">
        <div ui-view="AnnContainer"></div>
    </div>
